---
---
<div class="hero">
    <div class="container">
        <div class="hero-content">
            <h1>Baumeister Andreas Bauer</h1>
            <h2>Allgemein beeideter und gerichtlich zertifizierter Sachverständiger</h2>
            <div class="hero-cta">
                <a href="/kontakt" class="btn btn-primary">Kontakt aufnehmen</a>
            </div>
        </div>
    </div>
    <div class="hero-overlay"></div>
    <div class="hero-fade"></div>
</div>

<section class="services">
    <div class="container">
        <div class="section-header">
            <h2 class="section-title">Meine Leistungen</h2>
            <div class="accent-line"></div>
        </div>
        <div class="service-grid">
            <div class="service-card">
                <div class="card-icon">🎓</div>
                <h3>Schulungen</h3>
                <p>Schulungen für WDVS Firmen mit Fokus auf spezifische bauphysikalische und bauchemische Vorgänge</p>
                <a href="/schulungen" class="btn-text">Mehr erfahren <span class="arrow">→</span></a>
            </div>

            <div class="service-card">
                <div class="card-icon">📊</div>
                <h3>Gutachten</h3>
                <p>Gerichtlich beeideter Sachverständiger für Bauphysik, Bauschäden und mehr</p>
                <a href="/gutachten" class="btn-text">Mehr erfahren <span class="arrow">→</span></a>
            </div>

            <div class="service-card">
                <div class="card-icon">✓</div>
                <h3>Qualitätskontrolle</h3>
                <p>Begleitende Qualitätskontrolle für Bauvorhaben aller Art</p>
                <a href="/qualitaetskontrolle" class="btn-text">Mehr erfahren <span class="arrow">→</span></a>
            </div>

            <div class="service-card">
                <div class="card-icon">💡</div>
                <h3>Beratung</h3>
                <p>Professionelle Beratung zu allen bautechnischen Fragen</p>
                <a href="/beratung" class="btn-text">Mehr erfahren <span class="arrow">→</span></a>
            </div>
        </div>
    </div>
</section>

<section class="about">
    <div class="container">
        <div class="about-content">
            <div class="about-text">
                <div class="section-header">
                    <h2>Über mich</h2>
                    <div class="accent-line"></div>
                </div>
                <p>Als allgemein beeideter und gerichtlich zertifizierter Sachverständiger biete ich Ihnen mein
                    umfassendes Fachwissen und langjährige Erfahrung in allen Bereichen des Bauwesens.</p>
                <p>Ob Schulungen, Gutachten, Qualitätskontrollen oder Beratungen - ich stehe Ihnen mit meiner Expertise
                    zur Verfügung.</p>
                <a href="/persoenliches" class="btn btn-gradient">Mehr über mich</a>
            </div>
        </div>
    </div>
    <div class="about-bg"></div>
</section>

<section class="contact-section">
    <div class="container">
        <div class="contact-card">
            <div class="section-header centered">
                <h2>Kontaktieren Sie mich</h2>
                <div class="accent-line"></div>
            </div>
            <p>Für Anfragen stehe ich Ihnen gerne zur Verfügung.</p>
            <a href="/kontakt" class="btn btn-primary">Zum Kontaktformular</a>
        </div>
    </div>
</section>

<style>
    /* Hero Section */
    .hero {
        position: relative;
        color: var(--light-text);
        padding: 8rem 0 6rem;
        text-align: center;
        background-image: url('https://images.unsplash.com/photo-1503387762-592deb58ef4e?ixlib=rb-1.2.1&auto=format&fit=crop&w=1489&q=80');
        background-size: cover;
        background-position: center;
        margin-top: -1rem;
    }

    .hero-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(to right, rgba(18, 18, 18, 0.92), rgba(18, 18, 18, 0.6), rgba(18, 18, 18, 0.85));
        z-index: 0;
    }

    /* Smooth fade from hero into page background */
    .hero-fade {
        position: absolute;
        left: 0;
        bottom: -1px;
        width: 100%;
        height: 80px;
        background: linear-gradient(to bottom, rgba(20, 22, 24, 0), var(--primary-color));
        z-index: 0;
        pointer-events: none;
    }

    .hero-content {
        position: relative;
        z-index: 1;
        max-width: 800px;
        margin: 0 auto;
    }

    .hero h1 {
        font-size: 3rem;
        font-weight: 700;
        margin-bottom: 1rem;
        background: var(--accent-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        display: inline-block;
    }

    .hero h2 {
        font-size: 1.5rem;
        font-weight: 400;
        margin-bottom: 2rem;
        opacity: 0.9;
    }

    .hero-cta {
        display: flex;
        gap: 1rem;
        justify-content: center;
        margin-top: 2rem;
    }

    /* Section styles */
    .section-header {
        margin-bottom: 3rem;
    }

    .section-header.centered {
        text-align: center;
    }

    .section-title {
        font-size: 2.2rem;
        font-weight: 600;
        margin-bottom: 1rem;
        text-align: center;
    }

    .accent-line {
        height: 4px;
        width: 60px;
        background: var(--accent-gradient);
        margin: 0.5rem auto;
        border-radius: 2px;
    }

    /* Services Section */
    .services {
        padding: 6rem 0;
        position: relative;
    }

    .service-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
        padding: 0 1.5rem;
        margin: 0 auto;
        max-width: 1400px;
    }

    .service-card {
        background-color: var(--card-bg);
        padding: 2rem;
        border-radius: var(--border-radius);
        box-shadow: var(--box-shadow);
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
        z-index: 1;
    }

    .service-card::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 4px;
        background: var(--accent-gradient);
        z-index: 1;
        transition: height 0.3s ease;
    }

    .service-card:hover {
        transform: translateY(-10px);
        background-color: var(--card-hover);
    }

    .card-icon {
        font-size: 2.5rem;
        margin-bottom: 1.5rem;
        display: inline-block;
    }

    .service-card h3 {
        margin-bottom: 1rem;
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--light-text);
    }

    .service-card p {
        margin-bottom: 1.5rem;
        color: rgba(255, 255, 255, 0.8);
    }

    .btn-text {
        color: var(--accent-color);
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        font-weight: 500;
        transition: all 0.3s;
    }

    .btn-text .arrow {
        margin-left: 5px;
        transition: transform 0.3s;
    }

    .btn-text:hover .arrow {
        transform: translateX(5px);
    }

    /* About Section */
    .about {
        padding: 6rem 0;
        position: relative;
        overflow: hidden;
    }

    /* sanfter Übergang vom umgebenden Seitenhintergrund in den About-Bereich */
    .about::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 80px;
        background: linear-gradient(to bottom, var(--primary-color), rgba(20,22,24,0));
        z-index: 0;
        pointer-events: none;
    }

    .about-bg {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        /* dezenter, neutraler Hintergrund statt bunter Fläche */
        background: radial-gradient(1200px 500px at 80% -200px, rgba(96, 112, 134, 0.06), transparent 60%),
                    radial-gradient(800px 400px at 10% 120%, rgba(96, 112, 134, 0.04), transparent 60%);
        z-index: -1;
    }

    .about-content {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: 3rem;
        padding: 0 2rem;
        max-width: 1200px;
        margin: 0 auto;
        position: relative;
        z-index: 1;
    }

    .about-text {
        flex: 1;
        min-width: 300px;
    }

    .about-text h2 {
        font-size: 2.2rem;
        font-weight: 600;
        margin-bottom: 1rem;
        color: var(--light-text);
    }

    .about-text .accent-line {
        margin: 0.5rem 0 2rem;
    }

    .about-text p {
        margin-bottom: 1.5rem;
        font-size: 1.05rem;
        color: rgba(255, 255, 255, 0.8);
    }

    /* Contact Section */
    .contact-section {
        padding: 6rem 0;
        position: relative;
    }

    /* Sanfter Übergang an den Kanten des Kontakt-Bereichs */
    .contact-section::before,
    .contact-section::after {
        content: "";
        position: absolute;
        left: 0;
        width: 100%;
        height: 80px;
        pointer-events: none;
        z-index: 0;
    }

    /* Top-Fade: von Seitenhintergrund in den Kontaktbereich */
    .contact-section::before {
        top: 0;
        background: linear-gradient(to bottom, var(--primary-color), rgba(20,22,24,0));
    }

    /* Bottom-Fade: aus dem Kontaktbereich wieder in den Seitenhintergrund */
    .contact-section::after {
        bottom: 0;
        background: linear-gradient(to top, var(--primary-color), rgba(20,22,24,0));
    }

    .contact-card {
        background-color: var(--card-bg);
        padding: 3rem;
        border-radius: var(--border-radius);
        box-shadow: var(--box-shadow);
        text-align: center;
        max-width: 700px;
        margin: 0 auto;
        position: relative;
        overflow: hidden;
    }

    .contact-card::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(135deg, rgba(232, 62, 140, 0.05), rgba(40, 180, 120, 0.08), rgba(155, 48, 255, 0.05));
        z-index: 0;
    }

    .contact-card > * {
        position: relative;
        z-index: 1;
    }

    .contact-section h2 {
        margin-bottom: 1rem;
        font-size: 2.2rem;
        font-weight: 600;
        color: var(--light-text);
    }

    .contact-section p {
        margin-bottom: 2rem;
        font-size: 1.1rem;
        color: rgba(255, 255, 255, 0.8);
    }

    /* Buttons */
    .btn {
        display: inline-block;
        padding: 0.75rem 1.75rem;
        text-decoration: none;
        border-radius: 50px;
        font-weight: 500;
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
        z-index: 1;
    }

    .btn-primary {
        background: var(--accent-gradient);
        color: white;
        box-shadow: 0 4px 15px rgba(40, 180, 120, 0.3), 0 4px 15px rgba(232, 62, 140, 0.2);
    }

    .btn-primary:hover {
        transform: translateY(-3px);
        box-shadow: 0 7px 20px rgba(40, 180, 120, 0.4), 0 7px 20px rgba(232, 62, 140, 0.3);
    }

    .btn-outline {
        background: transparent;
        color: var(--light-text);
        border: 2px solid rgba(255, 255, 255, 0.3);
    }

    .btn-outline:hover {
        border-color: var(--accent-color);
        color: var(--accent-color);
        transform: translateY(-3px);
    }

    .btn-gradient {
        background: var(--accent-gradient);
        color: white;
        box-shadow: 0 4px 15px rgba(232, 62, 140, 0.3);
    }

    .btn-gradient:hover {
        transform: translateY(-3px);
        box-shadow: 0 7px 20px rgba(232, 62, 140, 0.4);
    }

    /* Responsive Design */
    @media (max-width: 768px) {
        .hero {
            padding: 6rem 0 4rem;
        }

        .hero h1 {
            font-size: 2.2rem;
        }

        .hero h2 {
            font-size: 1.2rem;
        }

        .hero-cta {
            flex-direction: column;
            gap: 1rem;
        }

        .section-title {
            font-size: 1.8rem;
        }

        .services, .about, .contact-section {
            padding: 4rem 0;
        }

        .contact-card {
            padding: 2rem;
        }
    }
</style>
---
